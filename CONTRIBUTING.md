# Contributing

Keep changes scoped to the active milestone. Update `LANGUAGE.md` before changing language semantics, add focused tests for behavior and malformed input, and run `cargo fmt` and `cargo test` before submitting. Do not add dependencies without documenting the need.
